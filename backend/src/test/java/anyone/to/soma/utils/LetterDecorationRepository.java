package anyone.to.soma.utils;

import anyone.to.soma.letter.domain.LetterDecoration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LetterDecorationRepository extends JpaRepository<LetterDecoration, Long> {
}
